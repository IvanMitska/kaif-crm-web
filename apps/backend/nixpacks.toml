[phases.install]
cmds = ["npm install -g corepack@0.24.1 && corepack enable && pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "cd apps/backend && npx prisma migrate reset --force --skip-seed && node dist/main.js"
