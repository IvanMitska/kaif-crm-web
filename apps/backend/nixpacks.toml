[phases.install]
cmds = ["npm install -g corepack@0.24.1 && corepack enable && pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "npx prisma db push --schema=./prisma/schema.prisma || true && node dist/main.js"
